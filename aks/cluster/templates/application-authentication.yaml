apiVersion: security.istio.io/v1beta1
kind: RequestAuthentication
metadata:
  name: jwt-validation
  namespace: default
spec:
  selector:
    matchLabels:
      auth: jwt
  jwtRules:
    - issuer: https://vtch-aks-demo.duckdns.org/auth/realms/master
      jwksUri: http://keycloak-http.default.svc.cluster.local/auth/realms/master/protocol/openid-connect/certs