apiVersion: v1
kind: Service
metadata:
  name: file-storage-api
  labels:
    name: file-storage-api
    app: file-storage
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    component: file-storage-api
