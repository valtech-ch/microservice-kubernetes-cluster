apiVersion: v1
kind: Service
metadata:
  name: authentication
  namespace: default
  labels:
    app: authentication
    service: authentication
spec:
  ports:
    - name: http
      port: 8090
      targetPort: 8080
  selector:
    app: authentication